# Vorlage fuer die lokale Entwicklung.
# Kopieren nach .env und Werte eintragen. Die Datei .env selbst wird nicht
# versioniert.

DEBUG=True
SECRET_KEY=lokaler-platzhalter-bitte-ersetzen

# Nur in der Produktion gesetzt: Verzeichnis des persistenten Volumes,
# in dem die Datenbankdatei liegt.
# DATA_DIR=/data
